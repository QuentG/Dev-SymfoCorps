<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('home') }}">SymfoCorps</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor02">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('offers_index') }}">{{ 'navbar.offers'|trans }}</a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item dropdown">
                <a aria-expanded="false" aria-haspopup="true" class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" id="navbarDropdown" role="button">
                    {{ app.request.locale }}
                </a>
                <div aria-labelledby="navbarDropdown" class="dropdown-menu">
                    {% for locale in locales %}
                        <a class="dropdown-item" href="{{ path('language_switch', { 'locale': locale }) }}">{{ locale }}</a>
                    {% endfor %}
                </div>
            </li>
            {% if not app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_register_choices') }}">{{ 'navbar.register'|trans }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">{{ 'navbar.login'|trans }}</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('account_index') }}">Profil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>